# Cloudflare Turnstile (get both from https://dash.cloudflare.com/?to=/:account/turnstile)
# Site key = public, served to frontend via /api/config so Turnstile shows online without frontend build env
# Secret key = private, used server-side to verify tokens. Keep .env out of version control!
TURNSTILE_SITE_KEY=your_site_key_here
TURNSTILE_SECRET_KEY=your_secret_key_here
